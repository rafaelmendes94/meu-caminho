VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# --- Gemini (backend / Edge Functions apenas) ---
# Opcional: chave configurada preferencialmente pelo Super Admin em /admin/ai/provider.
# Se preferir provisionar via env, defina esta variável no ambiente das Edge Functions.
# NUNCA prefixe com VITE_ e NUNCA exponha ao frontend.
GEMINI_API_KEY=
